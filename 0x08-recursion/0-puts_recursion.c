#include "main.h"
#include <stdio.h>
/**
 * _puts_recursion - Function that prints a sting and a new line
 * @s: Parameter of the function
 *
 * Return: There is no return value
 */

void _puts_recursion(char *s)
{
	printf("%s\n", s);
}
