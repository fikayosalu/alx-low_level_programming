#include "main.h"
/**
 * _strchr - Locates a character in a string
 * @s: The string
 * @c: The character to be located
 *
 * Return: Return a pointer to the first
 * ocurrence of the character
 */

char *_strchr(char *s, char c)
{
	int i;

	i = 0;
	while (s[i] != '\0')
	{

	}
}


int len(char *s)
{
	int i;
	length = 0;

	i = 0;
	while (s[i] != '\0')
	{
		i++;
	}
	return (i);
}
