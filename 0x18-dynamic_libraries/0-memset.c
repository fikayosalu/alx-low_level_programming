#include "main.h"

/**
 * _memset - function that prints characters
 * @s: Parameter of the function
 * @b: Parameter
 * @n: Parameter
 *
 * Return: Return 0
 */

char *_memset(char *s, char b, unsigned int n)
{
	return (s);
}
